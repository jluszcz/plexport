[project]
name = "plexport"
version = "0.1.0"
description = "Export statistics from your Plex library"
requires-python = ">=3.14"
dependencies = [
    "plexapi>=4.18.0",
]

[tool.uv]
package = true

[project.scripts]
plexport = "plexport.__main__:main"
